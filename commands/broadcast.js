exports.run = (message, bot) => {
  
}

exports.conf = {
  userPerm:["DEV"],
  botPerm:["SEND_MESSAGES"],
  coolDown:0,
  dm:true,
  category:"DevOnly",
  help:"Broadcast to all the servers",
  args:"",
}