const request = require('request');

exports.run = (message, bot, send) => {
    
}

exports.conf = {
  userPerm:[],
  botPerm:["SEND_MESSAGES"],
  coolDown:0,
  dm:true,
  category:"Filter",
  help:"",
  args:"",
}